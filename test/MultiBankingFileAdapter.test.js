import test from 'ava'
// import InMemoryCache from '../dist/lib/caches/InMemoryCache'
// import MultiBankingFileAdapter from '../dist/lib/MultiBankingFileAdapter'

// const testConfig = {
//   server: 'http://localhost:3000/',
//   cache: new InMemoryCache()
// }

// test('MultiBankingFileAdapter', t => {

//   const adapter = new MultiBankingFileAdapter(testConfig)
//   t.true(adapter instanceof Object)
// })
test('MultiBankingFileAdapter', t => {

  t.true(true)
})
